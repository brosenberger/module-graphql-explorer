<?php
/**
 * @var \Magento\Framework\View\Element\Template $block
 * @var \Magento\Framework\Escaper $escaper
 */
/** @var \BroCode\GraphQlExplorer\ViewModel\ExplorerConfig $config */
$config = $block->getData('explorer_config');
$stores = $config->isStoreSwitchAllowed() ? $config->getStoreOptions() : [];
?>
<div class="brocode-gqle">
    <div class="brocode-gqle__config">

        <!-- Column 1: where the queries go. -->
        <div class="brocode-gqle__col">
        <?php if ($stores): ?>
            <div class="brocode-gqle__row">
                <span class="brocode-gqle__label"><?= $escaper->escapeHtml(__('Store view')) ?></span>
                <select id="brocode-gqle-store" class="select brocode-gqle__select">
                    <?php foreach ($stores as $store): ?>
                        <option value="<?= $escaper->escapeHtmlAttr($store['code']) ?>"
                            <?= $store['code'] === $config->getCurrentStoreCode() ? 'selected="selected"' : '' ?>>
                            <?= $escaper->escapeHtml($store['label']) ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>
        <?php endif; ?>

            <div class="brocode-gqle__row">
                <span class="brocode-gqle__label"><?= $escaper->escapeHtml(__('Endpoint')) ?></span>
                <input type="text"
                       id="brocode-gqle-endpoint"
                       class="input-text brocode-gqle__endpoint"
                       readonly="readonly"
                       aria-readonly="true"
                       value="<?= $escaper->escapeHtmlAttr($config->getCurrentEndpoint()) ?>"/>
                <button type="button"
                        id="brocode-gqle-endpoint-copy"
                        class="action secondary brocode-gqle__icon-btn"
                        title="<?= $escaper->escapeHtmlAttr(__('Copy endpoint URL')) ?>"
                        aria-label="<?= $escaper->escapeHtmlAttr(__('Copy endpoint URL')) ?>">
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor"
                     stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                     aria-hidden="true" focusable="false">
                    <rect x="9" y="9" width="11" height="11" rx="2"></rect>
                    <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                </svg>
                </button>
            </div>

            <p class="brocode-gqle__note">
                <?= $escaper->escapeHtml(
                    __('Follows the store switcher. Each store view has its own base URL, and its own endpoint.')
                ) ?>
            </p>
        </div>

        <!-- Column 2: who the queries run as. -->
        <div class="brocode-gqle__col">
            <div class="brocode-gqle__row">
                <span class="brocode-gqle__label"><?= $escaper->escapeHtml(__('Customer token')) ?></span>
                <input type="text"
                       id="brocode-gqle-token"
                       class="input-text brocode-gqle__input"
                       autocomplete="off"
                       spellcheck="false"
                       placeholder="<?= $escaper->escapeHtmlAttr(__('Empty = query as guest')) ?>"/>
                <button type="button"
                        id="brocode-gqle-token-clear"
                        class="action secondary brocode-gqle__icon-btn brocode-gqle__clear"
                        title="<?= $escaper->escapeHtmlAttr(__('Clear token and query as guest')) ?>"
                        aria-label="<?= $escaper->escapeHtmlAttr(__('Clear token and query as guest')) ?>">
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor"
                     stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                     aria-hidden="true" focusable="false">
                    <line x1="18" y1="6" x2="6" y2="18"></line>
                    <line x1="6" y1="6" x2="18" y2="18"></line>
                </svg>
                </button>
            </div>

            <p class="brocode-gqle__note">
                <?= $escaper->escapeHtml(
                    __('Sent as an Authorization: Bearer header, scoped to the selected store view. Magento has no admin token for GraphQL, so a customer login is the way in.')
                ) ?>
            </p>

            <details id="brocode-gqle-token-form" class="brocode-gqle__tokengen">
                <summary class="brocode-gqle__summary">
                    <?= $escaper->escapeHtml(__('Need one? Sign in as a customer')) ?>
                </summary>

                <div class="brocode-gqle__row">
                    <input type="email" id="brocode-gqle-email"
                           class="input-text brocode-gqle__input"
                           autocomplete="off"
                           placeholder="<?= $escaper->escapeHtmlAttr(__('Customer email')) ?>"/>
                    <input type="password" id="brocode-gqle-password"
                           class="input-text brocode-gqle__input"
                           autocomplete="new-password"
                           placeholder="<?= $escaper->escapeHtmlAttr(__('Customer password')) ?>"/>
                    <button type="button" id="brocode-gqle-token-get" class="action primary">
                        <?= $escaper->escapeHtml(__('Request token')) ?>
                    </button>
                    <button type="button"
                            id="brocode-gqle-token-help"
                            class="action secondary brocode-gqle__icon-btn brocode-gqle__help"
                            aria-expanded="false"
                            aria-controls="brocode-gqle-token-mutation"
                            title="<?= $escaper->escapeHtmlAttr(__('Show the mutation this runs')) ?>"
                            aria-label="<?= $escaper->escapeHtmlAttr(__('Show the mutation this runs')) ?>">
                        <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor"
                         stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                         aria-hidden="true" focusable="false">
                        <circle cx="12" cy="12" r="10"></circle>
                        <path d="M9.1 9a3 3 0 0 1 5.8 1c0 2-3 3-3 3"></path>
                        <line x1="12" y1="17" x2="12.01" y2="17"></line>
                    </svg>
                    </button>
                </div>

                <div id="brocode-gqle-token-mutation" class="brocode-gqle__snippet" hidden="hidden">
                    <p class="brocode-gqle__note">
                        <?= $escaper->escapeHtml(
                            __('Exactly what the button runs. Paste it into the editor, or send it from any HTTP client.')
                        ) ?>
                    </p>
<pre id="brocode-gqle-token-mutation-code" class="brocode-gqle__code"><code>mutation {
  generateCustomerToken(
    email: "customer@example.com"
    password: "your-password"
  ) {
    token
  }
}</code></pre>
                    <button type="button" id="brocode-gqle-token-mutation-copy" class="action secondary">
                        <?= $escaper->escapeHtml(__('Copy mutation')) ?>
                    </button>
                </div>
            </details>

            <div id="brocode-gqle-token-status" class="brocode-gqle__messages" role="status" aria-live="polite"></div>
        </div>
    </div>

    <div id="brocode-gqle-root"
         class="brocode-gqle__root"
         data-gqle-config="<?= $escaper->escapeHtmlAttr($config->getJsonConfig()) ?>"></div>
</div>

<script type="text/x-magento-init">
{
    "#brocode-gqle-root": {
        "BroCode_GraphQlExplorer/js/explorer": {}
    }
}
</script>
